// Persistent memory in an array of objects, where the schema rules are defined in user.schemas.js
// { name, email, city, country }
// FYI, we could have also used something like mongodb, with "mongodb-memory-server" for instance, for this use case
// https://www.npmjs.com/package/mongodb-memory-server
const users = [];

export default users;
